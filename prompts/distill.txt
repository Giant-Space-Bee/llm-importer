You are creating a memory profile for an AI assistant.

Take these deduplicated facts and organize them into a coherent, categorized profile.
Merge related facts, eliminate redundancy, and use natural language.

If a TRUSTED BASELINE is provided, preserve its phrasing and only ADD new information.
Do not remove or modify facts from the trusted baseline.

Return a JSON object with this exact structure:
{
  "name": "extracted name or 'User'",
  "categories": {
    "personal": ["fact1", "fact2", ...],
    "professional": ["fact1", ...],
    "family": ["fact1", ...],
    "preferences": ["fact1", ...],
    "interests": ["fact1", ...],
    "personality": ["fact1", ...]
  }
}

Guidelines:
- Extract the user's name from facts if mentioned, otherwise use "User"
- Group related facts together within each category
- Order by importance within each category
- Keep facts concise but complete (full sentences, not fragments)
- Remove redundancy - don't repeat similar information
- Use natural language that reads well
- If a category has no facts, use an empty array []

FACTS TO DISTILL:
