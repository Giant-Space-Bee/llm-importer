You are creating a memory profile for an AI assistant.

The facts below are already deduplicated. Your job is to SYNTHESIZE them into a polished, readable profile.

You MAY combine related facts into single sentences for better flow.
Example: "Has a Maverick" + "Maverick cost $45k" + "Payments are $833 biweekly"
  → "Owns a Ford Maverick purchased for $45k with $833 biweekly payments"

If a TRUSTED BASELINE is provided, preserve its phrasing and only ADD new information.
Do not remove or modify facts from the trusted baseline.

Return a JSON object with this exact structure:
{
  "name": "extracted name or 'User'",
  "categories": {
    "identity": [{"fact": "...", "period": "..."}],
    "values": [{"fact": "...", "period": "..."}],
    "emotions": [{"fact": "...", "period": "..."}],
    "relationships": [{"fact": "...", "period": "..."}],
    "growth": [{"fact": "...", "period": "..."}],
    "history": [{"fact": "...", "period": "..."}],
    "practices": [{"fact": "...", "period": "..."}],
    "shadows": [{"fact": "...", "period": "..."}]
  }
}

Category definitions:
- identity: Who they are - name, age, location, background, self-descriptions
- values: What matters - beliefs, principles, priorities, ethics
- emotions: Feeling patterns - recurring emotions, triggers, coping mechanisms
- relationships: People - family, friends, partners, pets, interpersonal dynamics
- growth: Goals - aspirations, dreams, what they're building toward
- history: Past - life events, milestones, formative experiences
- practices: Habits - routines, self-care, meditation, therapy, journaling
- shadows: Depths - recognized patterns to transform, fears, blocks, inner conflicts

Guidelines:
- Extract the user's name from facts if mentioned, otherwise use "User"
- COMBINE related facts about the same topic into cohesive sentences
- **Temporal Reasoning**:
  - Identify CURRENT vs PAST states based on the "period" field.
  - Rewrite PAST states in past tense (e.g. "Dated Sara (2008)").
  - Consolidate timelines (e.g. "Lived in Victoria (2010-2015), then moved to Vancouver").
  - Do NOT consolidate contradictory facts unless they are clearly a timeline of changes.
- Order by importance within each category (key facts first)
- Keep facts concise but complete (full sentences, not fragments)
- Use natural language that reads well as a profile
- If a category has no facts, use an empty array []

FACTS TO DISTILL:
