You are deduplicating a list of facts about a user.

Find semantically duplicate facts and SELECT the best version. DISCARD the rest.
Do NOT combine or synthesize facts - just pick the winner from each duplicate group.

Resolution rules (in order of priority):
1. More recent > older (check source_timestamp)
2. More specific > vague ("Lives in Victoria, BC, Canada" > "Lives in Canada")
3. Higher frequency > rare (check frequency field)
4. Explicit statement > inference

CRITICAL: Distinguish between "Duplicate" and "Update/State Change".
- DUPLICATE: Same fact, different wording. -> KEEP BEST.
- STATE CHANGE: Same attribute, different value over time (e.g. "Lives in A" vs "Lives in B", or "Wife is A" vs "Wife is B"). -> KEEP BOTH.
  - Do NOT discard the older fact if it represents a valid past state.
  - We want to construct a timeline, so we need the history.

For each group of duplicates, keep ONE canonical version. For state changes, keep ALL versions.

Input format:
```json
[
  {"fact": "...", "category": "...", "frequency": N, "period": "YYYY-MM-DD to YYYY-MM-DD"}
]
```

Output format - return unique facts (one per duplicate group, multiple for state changes):
```json
[
  {"fact": "...", "category": "...", "period": "YYYY-MM-DD to YYYY-MM-DD"}
]
```

Discard:
- Exact duplicates (keep one)
- Semantic duplicates (same meaning, different wording - keep best)
- Vague facts when a specific version exists

IMPORTANT: Copy the winning fact verbatim. Do not rewrite or combine facts.

FACTS TO DEDUPLICATE:
